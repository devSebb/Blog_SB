<nav class="h-40 shadow-lg bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-blue-400 to-emerald-400">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8 items-center mt-10 mb-4">
    <div class="relative flex items-center justify-between trans">
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start md:mt-0">
        <div class="flex flex-shrink-0 items-center">
          <%= link_to root_path do %>
            <%= image_tag "blog_logo.png", class: "mx-auto h-14 md:-translate-y-3" %>
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <% if user_signed_in? %>
              <%= link_to "Home", root_path, class: "bg-teal-600 text-white rounded-md px-3 py-2 text-md font-medium"%>
              <%= link_to "New Blog Post", new_blog_post_path, class: "text-gray-800 hover:bg-teal-600 hover:text-white rounded-md px-3 py-2 text-md font-medium"%>
            <% end %>
          </div>
        </div>
        <% if user_signed_in? %>
          <h1 class="font-bold text-4xl mx-auto md:-translate-x-6">A Blog on Ruby</h1>
        <% else %>
          <h1 class="font-bold text-4xl mx-auto md:translate-x-6">A Blog on Ruby</h1>
        <% end %>
      </div>
      <% if user_signed_in? %>
        <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0 md:mt-0 md:-translate-y-3">
          <div class="relative mr-3">
            <%= link_to "Profile", edit_user_registration_path, class: "text-gray-800 hover:bg-teal-600 hover:text-white rounded-md px-3 py-2 text-md font-medium" %>
          </div>
          <%= link_to "Log out", destroy_user_session_path, class: "text-gray-800 hover:bg-teal-600 hover:text-white rounded-md px-3 py-2 text-md font-medium", data: { "turbo-method": :delete } %>
        </div>
      </div>
    </div>
  <% else %>
    <div class="flex flex-shrink-0 items-center md:mt-0">
      <%= link_to "Log In", new_user_session_path, class: "text-gray-800 hover:bg-teal-600 hover:text-white rounded-md px-3 py-2 text-md font-medium" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-800 hover:bg-teal-600 hover:text-white rounded-md px-3 py-2 text-md font-medium" %>
    </div>
  <% end %>
</nav>
